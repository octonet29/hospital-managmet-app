import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const Rides = () => {
	return (
		<SafeAreaView className="bg-general-500">
			<Text>Hello This is Rides Page</Text>
		</SafeAreaView>
	)
}

export default Rides
